# PRD：我是教练 + 环球世界

> 版本：v1.0  
> 文档类型：产品需求文档（PRD）  
> 关联模块：我是教练（抽奖玩法）、环球世界（收集玩法）

---

## 1. 文档概述

### 1.1 产品简介
用户代入"教练"身份，在球场九宫格上进行点击式抽奖玩法（**我是教练**），通过连续抽奖成功逐步收集 48 个国家的足球卡片（**环球世界**），并解锁兑换商城限定权益。

### 1.2 模块关系
```
我是教练 (抽奖入口)
   │
   │ 连续 3 次成功 → 触发卡片掉落
   ▼
环球世界 (收集 + 权益解锁)
   │
   │ 集齐 16 / 32 / 48 个国家卡片
   ▼
商店 - 限量权益 (权益兑换)
```

### 1.3 核心数值字段
| 字段 | 含义 | 备注 |
| --- | --- | --- |
| `goal_total` | 用户历史累计获得 GOAL 值 | 展示用 |
| `goal_balance` | 当前可消耗 GOAL 值 | 抽奖扣减 |
| `goal_cost` | 单次抽奖消耗的 GOAL 值 | 配置项 |
| `success_progress` | 当前连续成功进度（0–3） | 满 3 触发卡片 |
| `card_owned` | 已收集国家卡片数（0–48） | |
| `right_goalofworld` | 商城可兑换权益次数（0–3） | 解锁 16/32/48 时 +1 |

---

## 2. 模块一：我是教练

### 2.1 需求概述
用户点击球场九宫格中的不同位置进行抽奖。每次抽奖消耗一定 GOAL 值；点击后系统以文字播报球场形势，并按概率掉落国家卡片，用于在"环球世界"中解锁。

### 2.2 视觉与布局

#### 2.2.1 底图
- 俯瞰视角的球场九宫格底图。
- 静态元素齐备：禁区、球门、中圈、两队球员等。
- 底部有简要玩法介绍文案。

#### 2.2.2 九宫格
- 将球场均匀分为 9 个可点击宫格。
- 1 个足球 ⚽️ 在各宫格之间**概率性动态出现并短暂停留**，体现球场形势变化。
- 每个宫格均可点击。

#### 2.2.3 形势播报卡片
- **静态图标**：麦克风 🎤
- **动态图标**：地球 🌍（持续旋转，作为按钮可点击）
- **进度条**：3 个并排足球 ⚽️
  - 默认：灰色
  - 进度 1：第 1 个球点亮为**铜色**
  - 进度 2：第 2 个球点亮为**银色**
  - 进度 3：第 3 个球点亮为**金色**
- **反馈文字区**：根据点击结果展示对应播报文案。

#### 2.2.4 GOAL 值展示
- 位置：所有页面**左上方"入场"按钮下方**。
- 内容：`goal_total`（历史获得） / `goal_balance`（可消耗），用不同颜色区分。
- 交互：长按悬浮卡片说明文案；每次点击宫格实时变化。

### 2.3 文案库

| 类型 | 数量 | 文案 |
| --- | --- | --- |
| 失败 | 6 | 被断球 / 传球失误 / 受伤 / 犯规 / 被铲断 / 拼抢失败 |
| 成功 | 6 | 大脚解围 / 短传渗透 / 禁区妙传 / 任意球 / 角球 / 界外球 |
| 抽中（卡片掉落） | 6 | 远射进球 / 头球得分 / 抢点得分 / 挑射进球 / 抽射远角 / 推射近角 |

> 抽中文案在"连续 3 次成功后"触发卡片掉落时使用，配合卡片动效。

### 2.4 交互逻辑

#### 2.4.1 点击宫格判定流程
```
点击宫格
   │
   ├─ GOAL 余额不足？──是──► 返回 "GOAL 值不足" 文案，不扣费、不计入进度
   │
   是 → 扣减 goal_cost
   │
   ├─ 该宫格当前是否有足球？
   │    │
   │    ├─ 否 ──► 判定【失败】
   │    │         随机返还失败文案 + 沉闷音效
   │    │         重置 success_progress = 0
   │    │
   │    └─ 是 ──► 按 X% 概率判定【成功】
   │              ├─ 成功：随机返还成功文案 + 明亮音效
   │              │        success_progress += 1
   │              │        点亮对应进度球（铜/银/金）
   │              │
   │              └─ (1-X)% 失败：同上失败分支
   │
   └─ 若 success_progress == 3：
         触发【抽中】文案 + 国家卡片掉落动效
         调用环球世界卡片分配逻辑
         success_progress 重置为 0
```

#### 2.4.2 其他点击交互
| 元素 | 行为 |
| --- | --- |
| 进度条 ⚽️⚽️⚽️ | 拉起说明弹层，展示游戏规则与当前进度状态 |
| 地球 🌍 按钮 | 拉起"环球世界"弹窗 |
| GOAL 值（长按） | 悬浮说明文案 |

### 2.5 状态规则
1. 用户需**连续 3 次成功**（进度条铜/银/金全亮）才能在"环球世界"中获得 1 张国家队卡片。
2. **GOAL 值校验**：`goal_balance < goal_cost` 时不允许抽奖，返还"GOAL 值不足"文案。
3. **国家卡片抽取概率**：在尚未被该用户抽到的国家中**等概率随机**分配，单卡概率 = `1 / (48 - card_owned)`。
4. 失败会**清零** `success_progress`（待确认是否保留累计进度，建议清零以提高玩法刺激度）。

---

## 3. 模块二：环球世界

### 3.1 需求概述
"我是教练"抽奖成功后获得国家队卡片，作为收集玩法承接。用户通过不断抽奖成功，逐步收集 48 个国家的足球，并解锁阶梯式权益。

### 3.2 视觉与布局

#### 3.2.1 弹窗主体
- 形式：**半屏弹窗**。
- 布局：48 个坑位组成一个**球形**整体；每个坑位为圆形。
- 坑位状态：
  | 状态 | 视觉 |
  | --- | --- |
  | 已收集 | 填充对应**球形国旗** |
  | 未收集（普通） | 显示"？" |
  | 未收集（解锁节点） | 第 **16 / 32 / 48** 个坑位显示 🔒 |

#### 3.2.2 三档分区
- 48 个坑位均分为**上 / 中 / 下** 3 个区域，每区域 16 个国家。
- 每个区域使用不同弹窗底色，对应 3 档权益解锁进度。

#### 3.2.3 国家队卡片
- 底图：国家国旗
- 文案：粗黑字体国家名称、足球文化与历史介绍、当家球星介绍
- 动效：边框闪耀

#### 3.2.4 数值条
- 已收集数量 / 总数（如 `12 / 48`）
- 商城可兑换权益次数 `right_goalofworld`（0–3 次）

#### 3.2.5 解锁权益卡片
- 触发：集齐第 16 / 32 / 48 个坑位时。
- 文案："恭喜解锁兑换商城限定礼物权益 1 次"
- 按钮：
  - 「前往领取」→ 跳转 **商店 - 限量权益**
  - 「继续收集」→ 关闭卡片
  - 右上角 ✕ → 关闭卡片
- 动效：卡片弹起 + 解锁动效。

### 3.3 交互逻辑

#### 3.3.1 入口
- 来源 1：在"我是教练"模块点击地球 🌍 按钮 → 拉起弹窗。
- 来源 2：连续 3 次成功掉落卡片 → 自动拉起弹窗（先播卡片动效，再填入坑位）。

#### 3.3.2 坑位点击
| 坑位状态 | 点击行为 |
| --- | --- |
| ？（未收集） | 浮窗提示："通过『我是教练』抽奖获得球形国旗" |
| 🔒（区域锁） | 浮窗提示："收集齐该区域 16 个球形国旗，将获得兑换商城权益资格" |
| 球形国旗（已收集） | 弹起对应国家队卡片详情 |

#### 3.3.3 卡片掉落填入
1. "我是教练"抽奖成功后，弹起国家卡片并播放**统一动效**。
2. 找到 48 个坑位中**下一个未被填充**的坑位填入对应球形国旗。
3. 若填入后 `card_owned == 16 / 32 / 48`：
   - 弹起【解锁权益卡片】并播放动效；
   - 数值条权益次数更新为 `right_goalofworld + 1`。

### 3.4 状态规则

#### 3.4.1 核心规则
- 卡片抽取顺序与坑位填入顺序一一对应（按抽中先后顺序填入下一个空位）。
- `right_goalofworld` 上限为 3，对应 16/32/48 三档解锁。

#### 3.4.2 待补充事项 ⚠️
以下边界状态在当前需求中尚未明确，需产品/运营补齐：

| 编号 | 待补充项 | 建议方向 |
| --- | --- | --- |
| Q1 | 未获得 / 已获得 / 重复获得 / 已集齐 / 奖励已领取 / 奖励已过期 各状态的视觉与文案 | 列举状态机 |
| Q2 | 重复抽中已拥有的国家时如何处理 | 转化为 GOAL / 碎片 / 补偿奖励 / 不处理（四选一） |
| Q3 | 权益领取的有效期与过期回收策略 | 建议设置过期时间 |

> 注：根据 §2.5 第 3 条，当前抽取算法仅在"未获得"国家中分配，因此理论上**不会出现重复**；如规则调整为全集随机，则 Q2 必须明确。

---

## 4. 全局校验与异常分支

| 场景 | 处理方式 |
| --- | --- |
| GOAL 值不足 | 弹出"GOAL 值不足"文案，不扣费、不计入进度 |
| 网络异常 / 接口失败 | 抽奖请求失败提示重试，不扣减 GOAL |
| 已集齐 48 个国家后再次成功 3 次 | 待补充（建议：转为 GOAL 返还或不再触发卡片） |
| 多端同时抽奖 | 后端以扣费成功结果为准，避免双花 |

---

## 5. 数据埋点建议（待评审）

| 事件 | 触发时机 | 关键属性 |
| --- | --- | --- |
| `coach_grid_click` | 点击宫格 | grid_id, has_ball, result(success/fail), goal_cost |
| `coach_progress_full` | 进度条达成 3/3 | — |
| `world_card_drop` | 卡片掉落 | country_id, card_owned_after |
| `world_unlock` | 解锁 16/32/48 | tier(1/2/3) |
| `world_right_claim` | 点击「前往领取」 | tier |

---

## 6. 变更记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| v1.0 | 2026-05-10 | 基于原始需求整理首版 PRD，结构化交互、状态、数值字段，并标注待补充项 |
